{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Dry Run",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/dry_run.py",
            "console": "integratedTerminal",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        }
    ]
}